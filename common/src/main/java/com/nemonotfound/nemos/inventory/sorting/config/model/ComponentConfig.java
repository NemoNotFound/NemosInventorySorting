package com.nemonotfound.nemos.inventory.sorting.config.model;

public record ComponentConfig(String componentName, boolean isEnabled, Integer xOffset, Integer rightXOffset, Integer yOffset, int width,
                              int height) {
}
